from setuptools import find_packages, setup

setup(name="corator",
      version="0.1",
      author="Daniel Berenberg",
      description="Stylish decorators for coding with your nose up",
      packages=find_packages())
